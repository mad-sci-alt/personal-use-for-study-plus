---
tags:
  - 操作系统
  - 状态转移
---
#### 引入：操作系统没有中断会发生什么？

##### 如果没有中断机制，我们cpu在管理外设（键盘鼠标等）时需要不断轮询询问你是否按下了，这样效率就会极其低下，不如被动等待你来通知我，中断让cpu从轮询中解放出来

#### 因此引入中断的概念
- 中断在进程状态转换的应用：进程在内存中缺少指定资源需要去磁盘中进行io操作，在阻塞态等待时，不能无限等待下去，在目标数据从磁盘中取出之后，发出一个硬件中断通知进程，进程被唤醒，转为就绪态
