---
tags:
  - 操作系统
  - cs408
---
##### 系统中进程的五大状态
- 一开始进程被创建系统开始为进程分配所需资源
- 当进程所有的资源被分配到位（除了cpu调度以外），转为就绪态
- 当进程优先级可以被cpu调度，分配cpu调度，转为运行态
- 进程运行时的时间片用完或者cpu更高优先级的进程进入就绪态，当前进程转为就绪态
- 运行中的进程也可以主动挂起，等待特定事件发生（比如等待用户输入...），进程转为阻塞态
- 阻塞态等待的事件发生，进程重新回到就绪态等待cpu调度
- 运行中的进程任务全部完成后进入终止态，终止态的进程不会再继续改变状态